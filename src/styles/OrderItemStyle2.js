import { StyleSheet } from "react-native";

const OrderItemStyle2 = StyleSheet.create({
    container: {
        padding: 10
    },
    header: {
        flexDirection: 'row',
        justifyContent: 'space-between'
    },
    flat: {
        marginTop: 10,
        elevation:5
    }
})

export default OrderItemStyle2